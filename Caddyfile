{$DOMAIN} {
    reverse_proxy app:8000
    tls /ssl/fullchain.crt /ssl/privkey.key

    log {
        output file /var/log/caddy/access.log
    }
}

# Редирект HTTP → HTTPS
:80 {
    redir https://{host}{uri}
}
